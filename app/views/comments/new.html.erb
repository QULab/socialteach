<div class="container">
	<div class="col-md-8 col-md-offset-2">
		<h1>New comment</h1>

		<% if @comment.parent %>
		  <%= render 'comment', comment: @comment.parent, from_reply_form: true %>
		<% end %>

		<%= render 'form' %>
	</div>
</div>
